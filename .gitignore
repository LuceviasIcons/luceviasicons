# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# SSH-ключи деплоя лежат в рабочей папке — в гит они попасть не должны
WSCode
WSCode.pub
*.pem
id_rsa*

# Источник истины — SVG в packages/core/svg, производные не коммитим.
packages/icons/src

# Исключение — packages/core/assets/icons.json: сайт ставит @lucevias/core
# прямо из этого репозитория, а npm при установке из git берёт только то,
# что реально закоммичено, и генераторы не запускает. Файл собирается
# скриптом build-core.mjs — руками не править.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
